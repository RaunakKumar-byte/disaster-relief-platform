<%- include('partials/header') %>
<div class="container mx-auto px-4 py-12">
  <div class="max-w-lg mx-auto bg-white rounded-lg shadow-md p-8">
    <h1 class="text-2xl font-bold text-red-600 mb-4"><%= title || 'Error' %></h1>
    <p class="text-gray-700 mb-4"><%= message || (error && error.message) || 'An unexpected error occurred.' %></p>
    <a href="/" class="text-primary hover:underline">Go back home</a>
  </div>
</div>
<%- include('partials/footer') %> 